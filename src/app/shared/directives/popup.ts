import { AfterViewInit, Directive, ElementRef, HostListener, inject, Input, input, Renderer2 } from '@angular/core';

@Directive({
  selector: '[appPopup]'
})
export class Popup{
 
//   private readonly el = inject(ElementRef);
//   private readonly renderer2 = inject(Renderer2);

//   myDiv!:HTMLElement;

//   @Input() msg!:string;

// @HostListener('mouseenter')
//   addPopup():void{
//      this.renderer2.setStyle(this.el.nativeElement, 'position', 'relative');

//  this.myDiv = this.renderer2.createElement('div');

//  this.renderer2.setStyle(this.myDiv, 'position', 'absolute');
//  this.renderer2.setStyle(this.myDiv, 'left', '0');
//  this.renderer2.setStyle(this.myDiv, 'bottom', '100%');
//  this.renderer2.setStyle(this.myDiv, 'padding', '10px 15px');
//  this.renderer2.setStyle(this.myDiv, 'border-radius', '7px');
//  this.renderer2.setStyle(this.myDiv, 'color', 'white');
//  this.renderer2.setStyle(this.myDiv, 'background-color', 'green');


//  this.myDiv.innerText = this.msg;


//  this.renderer2.appendChild(this.el.nativeElement, this.myDiv);

// }

// @HostListener('mouseleave')
// removePopup():void{
//   this.renderer2.removeChild(this.el.nativeElement, this.myDiv);
// }
  }

  
  


